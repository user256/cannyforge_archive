{
  "name": "cannyforge-archive",
  "private": true,
  "description": "JS unit tests for the CannyForge Archive admin settings page (assets/js/admin.js).",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@wordpress/env": "^11.11.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "<rootDir>/tests/js/**/*.test.js"
    ]
  }
}
